# Miraru — dependencias
# Versiones verificadas con Python 3.13 (usadas en la build de release)

# ── Núcleo ────────────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn[standard]>=0.30.0
pydantic==2.13.4
anyio>=4.0.0
python-multipart>=0.0.9

# ── Scrapers / red ────────────────────────────────────────────────────────────
# requests ya incluye urllib3, de donde sale el Retry con backoff (scrapers/net.py)
requests>=2.32.0
beautifulsoup4==4.14.3
lxml>=5.0.0

# ── Google Sheets (sincronización opcional) ───────────────────────────────────
google-auth>=2.30.0
google-api-python-client==2.196.0

# ── Notificaciones Web Push (llegan con la pestaña cerrada) ──────────────────
pywebpush>=2.0.0

# ── Imágenes (avatar, iconos del build) ───────────────────────────────────────
Pillow>=10.0.0

# ── Empaquetado ───────────────────────────────────────────────────────────────
pyinstaller>=6.0.0

# ── Desarrollo / tests ────────────────────────────────────────────────────────
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.6.0

# ── Opcionales ────────────────────────────────────────────────────────────────
# Discord Rich Presence: si falta, la función se desactiva sola (no rompe nada).
pypresence>=4.3.0
